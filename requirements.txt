# ============================================================
# 个性化旅游系统 - 项目依赖
# ============================================================
# 安装: pip install -r requirements.txt
# 最低 Python 版本: 3.7+

# --------------------- Web 框架 ---------------------
Flask>=2.0.1                # Web框架 + Jinja2 模板引擎

# --------------------- HTTP 请求 ---------------------
requests>=2.25.0            # HTTP 客户端（视频下载等）

# --------------------- AI 视频生成 ---------------------
volcengine-python-sdk       # 火山引擎 SDK（即梦AI视频生成 + Maas LLM）

# --------------------- AI Agent ---------------------
openai>=1.0.0               # OpenAI SDK（兼容 DeepSeek API）

# --------------------- 数据处理（可选，推荐算法预留）---------------------
# 以下依赖当前版本未直接使用，如需启用推荐算法可安装：
# scikit-learn>=1.0.2       # 推荐算法 (TF-IDF)
# pandas>=1.3.3             # CSV 数据处理
# numpy>=1.21.2             # 数值计算

# --------------------- 标准库（无需安装）---------------------
# os, sys, json, csv, uuid, heapq, collections, datetime,
# functools, hashlib, pickle, random, time, bisect, typing
